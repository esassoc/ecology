---
import DocsLayout from '../../layouts/DocsLayout.astro';
import { withBase } from '@esa/docs/base';
import Section from '@esa/docs/Section.astro';

const transitions = [
  { token: '--transition-fast', value: '150ms ease', role: 'Hover, focus, small state flips', cls: 'fast' },
  { token: '--transition-base', value: '200ms ease', role: 'Default for most interactions', cls: 'base' },
  { token: '--transition-slow', value: '350ms ease', role: 'Panels, overlays, larger moves', cls: 'slow' },
];
---
<DocsLayout title="Motion — Ecology" activeHref="/foundations/motion">
  <nav class="crumbs"><a href={withBase('/')}>Ecology</a> <span>/</span> <span>Foundations</span> <span>/</span> <span>Motion</span></nav>
  <h1>Motion</h1>
  <p class="lede">Three transition tokens cover the whole system. They share an easing curve and differ only in duration — fast for direct feedback, slow for larger spatial moves. <strong>Hover a swatch</strong> to feel its timing.</p>

  <Section id="transitions" title="Transitions">
    <p>Each swatch uses its token for both the color shift and the slide. Hover to trigger; the timing is the difference.</p>
    <div class="swatches">
      {transitions.map((t) => (
        <div class="swatch">
          <div class={`swatch__demo swatch__demo--${t.cls}`}>
            <div class="swatch__thumb"></div>
            <span class="swatch__hint">Hover me</span>
          </div>
          <div class="swatch__meta">
            <code>{t.token}</code>
            <span class="swatch__value">{t.value}</span>
            <span class="swatch__role">{t.role}</span>
          </div>
        </div>
      ))}
    </div>
  </Section>

  <Section id="usage" title="Usage">
    <p>Bind the token on the <code>transition</code> property and name the properties you animate — never animate <code>all</code>. Default to <code>--transition-base</code> unless the change is tiny (fast) or spatial (slow).</p>
    <pre class="snippet"><code>{`.button {
  transition: background var(--transition-fast),
              box-shadow var(--transition-fast);
}
.drawer {
  transition: transform var(--transition-slow);
}`}</code></pre>
  </Section>
</DocsLayout>

<style>
  .swatches { display: grid; grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); gap: var(--spacing-400, 1rem); }
  .swatch { border: 1px solid var(--color-border, #e5e5e5); border-radius: 10px; padding: var(--spacing-400, 1rem); }
  .swatch__demo {
    position: relative;
    height: 72px;
    border-radius: var(--radius-300, 0.5rem);
    background: var(--color-surface-sunken, #f5f5f5);
    display: flex; align-items: center;
    padding: 0 var(--spacing-300, 0.75rem);
    margin-bottom: var(--spacing-300, 0.75rem);
    overflow: hidden;
    cursor: pointer;
  }
  .swatch__thumb {
    width: 40px; height: 40px; border-radius: 8px;
    background: var(--color-primary, #43608a);
    transform: translateX(0);
  }
  .swatch__hint {
    margin-left: auto;
    font-size: 13px; color: var(--color-text-muted, #737373);
    transition: opacity var(--transition-fast, 150ms ease);
  }
  /* Each demo applies its own token to the animated thumb + background. */
  .swatch__demo--fast .swatch__thumb { transition: transform var(--transition-fast, 150ms ease), background var(--transition-fast, 150ms ease); }
  .swatch__demo--base .swatch__thumb { transition: transform var(--transition-base, 200ms ease), background var(--transition-base, 200ms ease); }
  .swatch__demo--slow .swatch__thumb { transition: transform var(--transition-slow, 350ms ease), background var(--transition-slow, 350ms ease); }
  .swatch__demo:hover .swatch__thumb { transform: translateX(calc(100% + var(--spacing-500, 1.5rem))); background: var(--color-accent, #f9a134); }
  .swatch__demo:hover .swatch__hint { opacity: 0; }

  .swatch__meta { display: flex; flex-direction: column; gap: 2px; }
  .swatch__meta code { font-family: var(--font-mono, monospace); font-size: 12.5px; color: var(--color-text-primary, #171717); }
  .swatch__value { font-family: var(--font-mono, monospace); font-size: 12.5px; color: var(--color-text-secondary, #525252); }
  .swatch__role { font-size: 12.5px; color: var(--color-text-muted, #737373); }

  .snippet { background: var(--color-surface-sunken, #f5f5f5); border: 1px solid var(--color-border, #e5e5e5); border-radius: 10px; padding: var(--spacing-400, 1rem) var(--spacing-500, 1.5rem); overflow-x: auto; }
  .snippet code { font-family: var(--font-mono, monospace); font-size: 13.5px; line-height: 1.7; color: var(--color-text-primary, #171717); }

  @media (prefers-reduced-motion: reduce) {
    .swatch__thumb, .swatch__hint { transition: none !important; }
  }
</style>
