{
  "name": "@esa/storybook-astro",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Storybook workbench for the .astro half of @esa/ecology. Separate from @esa/storybook because Storybook allows exactly one framework per instance — the two are stitched together with composition (refs).",
  "scripts": {
    "dev": "storybook dev -p 6007 --no-open",
    "build": "storybook build -o dist"
  },
  "dependencies": {
    "@esa/ecology": "*",
    "@esa/tokens": "*"
  },
  "devDependencies": {
    "@storybook-astro/framework": "^1.10.0",
    "@storybook/addon-a11y": "^10.5.8",
    "@storybook/addon-docs": "^10.5.8",
    "astro": "^5.18.2",
    "storybook": "^10.5.8",
    "vite": "^6.0.0"
  }
}
