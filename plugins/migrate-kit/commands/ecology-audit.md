Audit an existing Angular app's reusable UI and map it onto the Ecology design
system — inventory → DIRECT / PARTIAL / COEXIST / GAP mapping → token + library
gap analysis → an ordered, two-track migration plan — plus the safe,
design-independent token-alias scaffolding. Spoke-driven when `--spoke` is given.

$ARGUMENTS

Load the **`ecology-audit`** skill and follow its phases, passing through any
`--app` / `--ecology` / `--out` / `--spoke` / `--no-scaffold` arguments above.
The audit is read-only except for the additive token aliases + the generated
docs under `<out>/`; re-run it for a burndown.
